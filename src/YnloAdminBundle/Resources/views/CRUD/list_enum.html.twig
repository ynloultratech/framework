{% extends admin.getTemplate('base_list_field') %}

{% block field %}
    {% if value is not null %}
        {{ value|readable_enum(field_description.options.enum_type)|trans }}
    {% endif %}
{% endblock %}
